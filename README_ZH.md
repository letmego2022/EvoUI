# EvoUI – 自进化智能 UI 自动化

**EvoUI** 是一个先进的 UI 自动化框架，结合了 AI 驱动的操作记忆、视觉兜底执行以及智能步骤验证。系统能够从历史操作中学习，复用已有操作记忆，并自适应动态变化的 Web 界面。  

---

## 功能特点

### 1. AI 驱动的操作记忆
- 可以复用之前的 UI 操作以完成新的步骤，高置信度选择最佳动作。
- 支持参数化复用（`reuse_modified`），可调整输入文本等参数。
- 避免盲目重复操作，综合考虑元素坐标、动作类型与上下文。
- 记录每个操作的成功/失败统计及最后使用时间。

### 2. 多动作步骤验证
- 每个步骤可以包含多个动作（type、click、select 等）。
- EvoUI 会验证**每个动作**是否成功执行。
- 可检测未完成的步骤，例如输入完成但保存按钮未点击。

### 3. 视觉反馈与兜底执行
- 集成 SoM（Semantic Overlay Marking）技术，精准定位 UI 元素。
- 可在页面上标记点击位置，便于调试。
- 如果记忆动作执行失败，会自动触发视觉兜底执行。
- 支持动态修正，当目标元素部分隐藏或页面未完全显示时仍能操作。

### 4. 状态翻转与输入一致性检查
- 检查开关、添加/移除按钮等状态翻转元素。
- 验证输入内容与预期一致，包括密码框等安全字段。

### 5. 自进化与操作治理
- 记录每个操作的历史成功/失败情况。
- AI 根据全局目标和步骤上下文选择最合适的操作。
- 系统通过学习可靠操作，提升自动化成功率。

---

## 系统架构

```

用户场景 / 测试用例
│
▼
步骤分解指令
│
▼
┌─────────────────────────┐
│  EvoUI 操作记忆层       │
│  - 复用已有步骤         │
│  - 参数化调整           │
└───────────┬─────────────┘
│
▼
动作执行引擎
┌─────────────────────────┐
│ - Click / Type / Scroll │
│ - Hover / Swipe / Keys  │
└───────────┬─────────────┘
│
▼
验证与视觉兜底
┌─────────────────────────┐
│ - Screenshot & SoM      │
│ - 步骤验证              │
│ - 视觉兜底执行          │
└─────────────────────────┘

````
操作记忆治理
<img width="1578" height="692" alt="image" src="https://github.com/user-attachments/assets/8e6216d0-91f9-4898-a506-2ded8744fa68" />

编辑与删除，虽然也可以新增， 不过一般都是AI新增
<img width="1766" height="1142" alt="image" src="https://github.com/user-attachments/assets/323dc550-f492-4357-b87a-ca2447f414a7" />

执行后报告
记忆驱动以及纯视觉驱动（SoM辅助）
<img width="1582" height="900" alt="image" src="https://github.com/user-attachments/assets/d219c227-6e69-4c92-a522-4c5527d37ad3" />

<img width="2525" height="8413" alt="image" src="https://github.com/user-attachments/assets/394700b6-d84b-427e-9f02-c2802f59c81b" />





